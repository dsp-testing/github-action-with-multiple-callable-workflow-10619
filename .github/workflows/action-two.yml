name: action-two

on:
  workflow_call:
    inputs: {} # if needed
    secrets: {} # if needed

jobs:
  submit-dependencies:
    runs-on: ubuntu-latest
    steps:
      - name: Display Message
        run: |
          echo "Hello from action two workflow!"
